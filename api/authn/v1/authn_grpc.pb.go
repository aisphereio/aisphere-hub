// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: authn/v1/authn.proto

package authnv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	AuthnService_LoginURL_FullMethodName   = "/authn.v1.AuthnService/LoginURL"
	AuthnService_Exchange_FullMethodName   = "/authn.v1.AuthnService/Exchange"
	AuthnService_Refresh_FullMethodName    = "/authn.v1.AuthnService/Refresh"
	AuthnService_LogoutURL_FullMethodName  = "/authn.v1.AuthnService/LogoutURL"
	AuthnService_Revoke_FullMethodName     = "/authn.v1.AuthnService/Revoke"
	AuthnService_Introspect_FullMethodName = "/authn.v1.AuthnService/Introspect"
	AuthnService_Me_FullMethodName         = "/authn.v1.AuthnService/Me"
)

// AuthnServiceClient is the client API for AuthnService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// AuthnService is the authentication facade for aisphere-hub.
//
// Hub does not issue or persist local access tokens. Browser login, OAuth code
// exchange, refresh, logout, token revocation and token introspection are
// delegated to the configured identity provider (Casdoor by default through
// aisphere-kit/kernel authn). API requests are authenticated by kit middleware
// using IdP access tokens.
//
// ## Browser-friendly 302 routes (MUST be registered by the implementation)
//
// In addition to the JSON RPCs declared below, implementations MUST register
// two browser-friendly HTTP routes that return a 302 redirect instead of a
// JSON body. These exist because copying a JSON-encoded URL out of a response
// body unescapes `&` into `\u0026` in many browsers and terminals, which
// breaks the redirect URL.
//
//	GET /v1/authn/login     -> 302 to the IdP login URL
//	GET /v1/authn/logout    -> 302 to the IdP logout URL
//
// They mirror the JSON `LoginURL` and `LogoutURL` RPCs but accept query
// parameters (redirect_uri, state, scope, prompt, post_logout_redirect_uri,
// id_token_hint) and perform the redirect server-side. They MUST be registered
// manually in the service layer via srv.HandleFunc, since google.api.http
// cannot express a 302 response. See the legacy backend
// `internal/service/auth.go` for the registration pattern.
//
// ## Multi-tenancy
//
// All requests carry optional `org_id` and `app_id` fields. When unset, the
// implementation falls back to the globally configured Casdoor organization
// and application. Per-request values allow multi-tenant deployments to
// target a specific org/app without restarting the service. This matches the
// kernel authn contracts (LoginURLRequest, AuthCodeExchangeRequest,
// RefreshTokenRequest, VerifyTokenRequest, RevokeTokenRequest all carry
// OrgID/AppID).
//
// ## Migration from legacy /v3/auth/*
//
// This service replaces the legacy `api.auth.v1.AuthService` at /v3/auth/*.
// Path is intentionally version-bumped to v1/authn to signal the contract
// change (Principal no longer duplicated on MeResponse top-level, new RPCs
// added). Deployments that need to keep legacy clients working should mount a
// gateway-level path rewrite /v3/auth/* -> /v1/authn/* for a transition
// period; the request/response bodies are wire-compatible except for the
// removed flat fields on MeResponse.
type AuthnServiceClient interface {
	// LoginURL returns the IdP authorization URL as JSON for SPA/debug usage.
	//
	// Browser users should prefer GET /v1/authn/login, which is registered as a
	// direct HTTP 302 route by the service implementation. See the service
	// comment above.
	LoginURL(ctx context.Context, in *LoginURLRequest, opts ...grpc.CallOption) (*LoginURLResponse, error)
	// Exchange exchanges an OAuth authorization code for IdP tokens.
	//
	// Supports PKCE via the optional `code_verifier` field for public clients
	// (SPAs, mobile apps) that cannot safely store a client secret. When the
	// authorization request was made with a `code_challenge`, the same
	// `code_verifier` MUST be supplied here.
	//
	// NOTE: kernel authn exposes CodeVerifier on AuthCodeExchangeRequest, but
	// the current Casdoor SDK adapter (kernel/authn/casdoor/token.go) does not
	// yet forward it to GetOAuthToken. The field is accepted on the wire for
	// forward compatibility; verify the kernel version before relying on PKCE
	// enforcement server-side.
	Exchange(ctx context.Context, in *ExchangeRequest, opts ...grpc.CallOption) (*ExchangeResponse, error)
	// Refresh refreshes an IdP access token using a refresh token.
	Refresh(ctx context.Context, in *RefreshRequest, opts ...grpc.CallOption) (*RefreshResponse, error)
	// LogoutURL returns the IdP logout URL as JSON for SPA/debug usage.
	//
	// Browser users can also call GET /v1/authn/logout for a direct HTTP 302.
	//
	// Side effect: token revocation. When the request carries an
	// `Authorization: Bearer <access_token>` header, OR an explicit
	// `access_token` field is supplied in the request body, the implementation
	// calls Revoke on that access token before returning the IdP logout URL.
	// This adds the token to the local session blacklist so subsequent API
	// calls with the same token are rejected even before the IdP session
	// expires. The `access_token` field makes this previously-implicit side
	// effect visible in the API contract.
	//
	// NOTE: kernel authn does not currently expose a LogoutURLBuilder
	// interface. The hub data layer must construct the Casdoor logout URL
	// itself using the configured Casdoor endpoint, client_id and
	// post_logout_redirect_uri. See legacy backend internal/data/auth.go for
	// the pattern (it built the URL via the old aisphere-kit authn adapter;
	// the new hub will need to call casdoor SDK directly or compose the URL
	// from config).
	LogoutURL(ctx context.Context, in *LogoutURLRequest, opts ...grpc.CallOption) (*LogoutURLResponse, error)
	// Revoke explicitly revokes an access or refresh token.
	//
	// Use this for multi-device session management (e.g. "sign out everywhere"
	// flows) where the caller wants to invalidate a token without navigating
	// the IdP logout page.
	//
	// IdP-side revocation is best-effort; the local session blacklist is
	// always updated so the token is rejected at the next API call regardless
	// of whether the IdP actually revokes it.
	//
	// NOTE: the current Casdoor SDK adapter (kernel/authn/casdoor/token.go)
	// returns ErrIdentityBackendFailed("casdoor sdk does not expose token
	// revocation"). Until the kernel adapter is updated, this RPC will only
	// update the local blacklist and return a warning. Callers that need
	// full IdP-side revocation should use LogoutURL with id_token_hint.
	Revoke(ctx context.Context, in *RevokeRequest, opts ...grpc.CallOption) (*RevokeResponse, error)
	// Introspect validates a token and returns the principal it represents.
	//
	// Useful for debug tools, third-party integrations, and service-to-service
	// calls that need to verify a token's validity and identity without going
	// through the full request middleware chain. Mirrors RFC 7662 token
	// introspection semantics (active=false when the token is invalid,
	// expired, or revoked).
	Introspect(ctx context.Context, in *IntrospectRequest, opts ...grpc.CallOption) (*IntrospectResponse, error)
	// Me returns the current authenticated principal.
	//
	// Requires `Authorization: Bearer <access_token>`. The returned Principal
	// includes `expires_at` so frontends can schedule proactive token refreshes
	// before the token expires, rather than waiting for a 401 to trigger a
	// reactive refresh.
	Me(ctx context.Context, in *MeRequest, opts ...grpc.CallOption) (*MeResponse, error)
}

type authnServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewAuthnServiceClient(cc grpc.ClientConnInterface) AuthnServiceClient {
	return &authnServiceClient{cc}
}

func (c *authnServiceClient) LoginURL(ctx context.Context, in *LoginURLRequest, opts ...grpc.CallOption) (*LoginURLResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(LoginURLResponse)
	err := c.cc.Invoke(ctx, AuthnService_LoginURL_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *authnServiceClient) Exchange(ctx context.Context, in *ExchangeRequest, opts ...grpc.CallOption) (*ExchangeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ExchangeResponse)
	err := c.cc.Invoke(ctx, AuthnService_Exchange_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *authnServiceClient) Refresh(ctx context.Context, in *RefreshRequest, opts ...grpc.CallOption) (*RefreshResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RefreshResponse)
	err := c.cc.Invoke(ctx, AuthnService_Refresh_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *authnServiceClient) LogoutURL(ctx context.Context, in *LogoutURLRequest, opts ...grpc.CallOption) (*LogoutURLResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(LogoutURLResponse)
	err := c.cc.Invoke(ctx, AuthnService_LogoutURL_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *authnServiceClient) Revoke(ctx context.Context, in *RevokeRequest, opts ...grpc.CallOption) (*RevokeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RevokeResponse)
	err := c.cc.Invoke(ctx, AuthnService_Revoke_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *authnServiceClient) Introspect(ctx context.Context, in *IntrospectRequest, opts ...grpc.CallOption) (*IntrospectResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(IntrospectResponse)
	err := c.cc.Invoke(ctx, AuthnService_Introspect_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *authnServiceClient) Me(ctx context.Context, in *MeRequest, opts ...grpc.CallOption) (*MeResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(MeResponse)
	err := c.cc.Invoke(ctx, AuthnService_Me_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// AuthnServiceServer is the server API for AuthnService service.
// All implementations must embed UnimplementedAuthnServiceServer
// for forward compatibility.
//
// AuthnService is the authentication facade for aisphere-hub.
//
// Hub does not issue or persist local access tokens. Browser login, OAuth code
// exchange, refresh, logout, token revocation and token introspection are
// delegated to the configured identity provider (Casdoor by default through
// aisphere-kit/kernel authn). API requests are authenticated by kit middleware
// using IdP access tokens.
//
// ## Browser-friendly 302 routes (MUST be registered by the implementation)
//
// In addition to the JSON RPCs declared below, implementations MUST register
// two browser-friendly HTTP routes that return a 302 redirect instead of a
// JSON body. These exist because copying a JSON-encoded URL out of a response
// body unescapes `&` into `\u0026` in many browsers and terminals, which
// breaks the redirect URL.
//
//	GET /v1/authn/login     -> 302 to the IdP login URL
//	GET /v1/authn/logout    -> 302 to the IdP logout URL
//
// They mirror the JSON `LoginURL` and `LogoutURL` RPCs but accept query
// parameters (redirect_uri, state, scope, prompt, post_logout_redirect_uri,
// id_token_hint) and perform the redirect server-side. They MUST be registered
// manually in the service layer via srv.HandleFunc, since google.api.http
// cannot express a 302 response. See the legacy backend
// `internal/service/auth.go` for the registration pattern.
//
// ## Multi-tenancy
//
// All requests carry optional `org_id` and `app_id` fields. When unset, the
// implementation falls back to the globally configured Casdoor organization
// and application. Per-request values allow multi-tenant deployments to
// target a specific org/app without restarting the service. This matches the
// kernel authn contracts (LoginURLRequest, AuthCodeExchangeRequest,
// RefreshTokenRequest, VerifyTokenRequest, RevokeTokenRequest all carry
// OrgID/AppID).
//
// ## Migration from legacy /v3/auth/*
//
// This service replaces the legacy `api.auth.v1.AuthService` at /v3/auth/*.
// Path is intentionally version-bumped to v1/authn to signal the contract
// change (Principal no longer duplicated on MeResponse top-level, new RPCs
// added). Deployments that need to keep legacy clients working should mount a
// gateway-level path rewrite /v3/auth/* -> /v1/authn/* for a transition
// period; the request/response bodies are wire-compatible except for the
// removed flat fields on MeResponse.
type AuthnServiceServer interface {
	// LoginURL returns the IdP authorization URL as JSON for SPA/debug usage.
	//
	// Browser users should prefer GET /v1/authn/login, which is registered as a
	// direct HTTP 302 route by the service implementation. See the service
	// comment above.
	LoginURL(context.Context, *LoginURLRequest) (*LoginURLResponse, error)
	// Exchange exchanges an OAuth authorization code for IdP tokens.
	//
	// Supports PKCE via the optional `code_verifier` field for public clients
	// (SPAs, mobile apps) that cannot safely store a client secret. When the
	// authorization request was made with a `code_challenge`, the same
	// `code_verifier` MUST be supplied here.
	//
	// NOTE: kernel authn exposes CodeVerifier on AuthCodeExchangeRequest, but
	// the current Casdoor SDK adapter (kernel/authn/casdoor/token.go) does not
	// yet forward it to GetOAuthToken. The field is accepted on the wire for
	// forward compatibility; verify the kernel version before relying on PKCE
	// enforcement server-side.
	Exchange(context.Context, *ExchangeRequest) (*ExchangeResponse, error)
	// Refresh refreshes an IdP access token using a refresh token.
	Refresh(context.Context, *RefreshRequest) (*RefreshResponse, error)
	// LogoutURL returns the IdP logout URL as JSON for SPA/debug usage.
	//
	// Browser users can also call GET /v1/authn/logout for a direct HTTP 302.
	//
	// Side effect: token revocation. When the request carries an
	// `Authorization: Bearer <access_token>` header, OR an explicit
	// `access_token` field is supplied in the request body, the implementation
	// calls Revoke on that access token before returning the IdP logout URL.
	// This adds the token to the local session blacklist so subsequent API
	// calls with the same token are rejected even before the IdP session
	// expires. The `access_token` field makes this previously-implicit side
	// effect visible in the API contract.
	//
	// NOTE: kernel authn does not currently expose a LogoutURLBuilder
	// interface. The hub data layer must construct the Casdoor logout URL
	// itself using the configured Casdoor endpoint, client_id and
	// post_logout_redirect_uri. See legacy backend internal/data/auth.go for
	// the pattern (it built the URL via the old aisphere-kit authn adapter;
	// the new hub will need to call casdoor SDK directly or compose the URL
	// from config).
	LogoutURL(context.Context, *LogoutURLRequest) (*LogoutURLResponse, error)
	// Revoke explicitly revokes an access or refresh token.
	//
	// Use this for multi-device session management (e.g. "sign out everywhere"
	// flows) where the caller wants to invalidate a token without navigating
	// the IdP logout page.
	//
	// IdP-side revocation is best-effort; the local session blacklist is
	// always updated so the token is rejected at the next API call regardless
	// of whether the IdP actually revokes it.
	//
	// NOTE: the current Casdoor SDK adapter (kernel/authn/casdoor/token.go)
	// returns ErrIdentityBackendFailed("casdoor sdk does not expose token
	// revocation"). Until the kernel adapter is updated, this RPC will only
	// update the local blacklist and return a warning. Callers that need
	// full IdP-side revocation should use LogoutURL with id_token_hint.
	Revoke(context.Context, *RevokeRequest) (*RevokeResponse, error)
	// Introspect validates a token and returns the principal it represents.
	//
	// Useful for debug tools, third-party integrations, and service-to-service
	// calls that need to verify a token's validity and identity without going
	// through the full request middleware chain. Mirrors RFC 7662 token
	// introspection semantics (active=false when the token is invalid,
	// expired, or revoked).
	Introspect(context.Context, *IntrospectRequest) (*IntrospectResponse, error)
	// Me returns the current authenticated principal.
	//
	// Requires `Authorization: Bearer <access_token>`. The returned Principal
	// includes `expires_at` so frontends can schedule proactive token refreshes
	// before the token expires, rather than waiting for a 401 to trigger a
	// reactive refresh.
	Me(context.Context, *MeRequest) (*MeResponse, error)
	mustEmbedUnimplementedAuthnServiceServer()
}

// UnimplementedAuthnServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedAuthnServiceServer struct{}

func (UnimplementedAuthnServiceServer) LoginURL(context.Context, *LoginURLRequest) (*LoginURLResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method LoginURL not implemented")
}
func (UnimplementedAuthnServiceServer) Exchange(context.Context, *ExchangeRequest) (*ExchangeResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Exchange not implemented")
}
func (UnimplementedAuthnServiceServer) Refresh(context.Context, *RefreshRequest) (*RefreshResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Refresh not implemented")
}
func (UnimplementedAuthnServiceServer) LogoutURL(context.Context, *LogoutURLRequest) (*LogoutURLResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method LogoutURL not implemented")
}
func (UnimplementedAuthnServiceServer) Revoke(context.Context, *RevokeRequest) (*RevokeResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Revoke not implemented")
}
func (UnimplementedAuthnServiceServer) Introspect(context.Context, *IntrospectRequest) (*IntrospectResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Introspect not implemented")
}
func (UnimplementedAuthnServiceServer) Me(context.Context, *MeRequest) (*MeResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Me not implemented")
}
func (UnimplementedAuthnServiceServer) mustEmbedUnimplementedAuthnServiceServer() {}
func (UnimplementedAuthnServiceServer) testEmbeddedByValue()                      {}

// UnsafeAuthnServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to AuthnServiceServer will
// result in compilation errors.
type UnsafeAuthnServiceServer interface {
	mustEmbedUnimplementedAuthnServiceServer()
}

func RegisterAuthnServiceServer(s grpc.ServiceRegistrar, srv AuthnServiceServer) {
	// If the following call pancis, it indicates UnimplementedAuthnServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&AuthnService_ServiceDesc, srv)
}

func _AuthnService_LoginURL_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(LoginURLRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).LoginURL(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_LoginURL_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).LoginURL(ctx, req.(*LoginURLRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuthnService_Exchange_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ExchangeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).Exchange(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_Exchange_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).Exchange(ctx, req.(*ExchangeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuthnService_Refresh_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RefreshRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).Refresh(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_Refresh_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).Refresh(ctx, req.(*RefreshRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuthnService_LogoutURL_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(LogoutURLRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).LogoutURL(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_LogoutURL_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).LogoutURL(ctx, req.(*LogoutURLRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuthnService_Revoke_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).Revoke(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_Revoke_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).Revoke(ctx, req.(*RevokeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuthnService_Introspect_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(IntrospectRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).Introspect(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_Introspect_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).Introspect(ctx, req.(*IntrospectRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _AuthnService_Me_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(MeRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(AuthnServiceServer).Me(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: AuthnService_Me_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(AuthnServiceServer).Me(ctx, req.(*MeRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// AuthnService_ServiceDesc is the grpc.ServiceDesc for AuthnService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var AuthnService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "authn.v1.AuthnService",
	HandlerType: (*AuthnServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "LoginURL",
			Handler:    _AuthnService_LoginURL_Handler,
		},
		{
			MethodName: "Exchange",
			Handler:    _AuthnService_Exchange_Handler,
		},
		{
			MethodName: "Refresh",
			Handler:    _AuthnService_Refresh_Handler,
		},
		{
			MethodName: "LogoutURL",
			Handler:    _AuthnService_LogoutURL_Handler,
		},
		{
			MethodName: "Revoke",
			Handler:    _AuthnService_Revoke_Handler,
		},
		{
			MethodName: "Introspect",
			Handler:    _AuthnService_Introspect_Handler,
		},
		{
			MethodName: "Me",
			Handler:    _AuthnService_Me_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "authn/v1/authn.proto",
}
