# Hub Frontend HTTP route — serves the Next.js frontend at hub.weagent.cc.
#
# NOTE: This route is defined for reference but the frontend is not yet
# deployed to K8s. Uncomment and apply when ready.
#
# The frontend talks directly to the hub API via NEXT_PUBLIC_HUB_URL
# (https://api.weagent.cc:30723), so no API rewrites are needed here.
# This route simply serves the static Next.js SPA.
#
# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: hub-front-route
#   namespace: aisphere
#   labels:
#     app.kubernetes.io/name: aisphere-hub-front
#     app.kubernetes.io/component: gateway-route
# spec:
#   parentRefs:
#     - name: aisphere-gateway
#       namespace: aisphere-system
#   hostnames:
#     - "hub.weagent.cc"
#   rules:
#     - matches:
#         - path:
#             type: PathPrefix
#             value: /
#       backendRefs:
#         - name: aisphere-hub-front
#           namespace: aisphere
#           port: 3000